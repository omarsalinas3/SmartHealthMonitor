<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Declarar que es una app de TV -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <!-- No requiere pantalla táctil -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name=".TvApplication"
        android:allowBackup="true"
        android:theme="@style/Theme.SmartHealthTV"
        android:banner="@drawable/banner_tv"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- TVActivity — Compose for TV + Navigation Compose (S12) -->
        <activity
            android:name=".TVActivity"
            android:exported="true"
            android:label="SmartHealth TV"
            android:theme="@style/Theme.SmartHealthTV"
            android:configChanges="keyboard|keyboardHidden|navigation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- LEANBACK_LAUNCHER: aparece en el launcher de Android TV -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity — Leanback BrowseSupportFragment (S11, conservada) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="SmartHealth TV Leanback"
            android:configChanges="keyboard|keyboardHidden|navigation" />

    </application>

</manifest>
